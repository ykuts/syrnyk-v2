<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    .container {
      max-width: 600px;
      margin: 0 auto;
      font-family: Arial, sans-serif;
      color: #333;
    }
    .header {
      background-color: #f8f9fa;
      padding: 20px;
      text-align: center;
      border-bottom: 3px solid #0056b3;
    }
    .order-details {
      margin: 20px 0;
      padding: 15px;
      border: 1px solid #dee2e6;
      border-radius: 5px;
    }
    .item {
      margin: 10px 0;
      padding: 10px;
      background: #f8f9fa;
      border-radius: 4px;
    }
    .total {
      font-size: 18px;
      font-weight: bold;
      margin: 15px 0;
      color: #0056b3;
    }
    .delivery-info {
      background: #e9ecef;
      padding: 15px;
      border-radius: 4px;
      margin: 15px 0;
    }
    .footer {
      margin-top: 30px;
      padding: 20px;
      text-align: center;
      font-size: 14px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>Merci pour votre commande !</h2>
      <p>Cher(e) {{firstName}},</p>
      <p>Votre commande #{{orderId}} a été passée avec succès.</p>
    </div>

    <div class="order-details">
      <h3>Détails de la commande :</h3>
      <div class="total">
        Montant total : {{totalAmount}} CHF
      </div>
      
      <h4>Articles :</h4>
      {{#each items}}
        <div class="item">
          <p><strong>{{product.name}}</strong></p>
          <p>Quantité : {{quantity}}</p>
          <p>Prix : {{price}} CHF</p>
        </div>
      {{/each}}

      <div class="delivery-info">
        <h4>Informations de livraison :</h4>
        <p><strong>Mode de livraison :</strong> {{deliveryDetails.type}}</p>
        {{#if deliveryDetails.station}}
        <p><strong>Gare :</strong> {{deliveryDetails.station}}</p>
        {{/if}}
        {{#if deliveryDetails.meetingPoint}}
        <p><strong>Point de rendez-vous :</strong> {{deliveryDetails.meetingPoint}}</p>
        {{/if}}
        {{#if deliveryDetails.stationMeetingTime}}
        <p><strong>Heure de rendez-vous :</strong> {{deliveryDetails.stationMeetingTime}}</p>
        {{/if}}
        <p><strong>Date de livraison :</strong> {{deliveryDetails.date}}</p>

        <h4>Mode de paiement :</h4>
        <p>{{paymentMethod}}</p>

        {{#if notesClient}}
        <div>
          <h4>Notes :</h4>
          <p>{{notesClient}}</p>
        </div>
        {{/if}}
      </div>
    </div>

    <div class="footer">
      <p>Nous vous informerons du statut de votre commande.</p>
      <p>Si vous avez des questions, n'hésitez pas à nous contacter.</p>
      <p>Cordialement,<br>Votre équipe SYRNYK</p>
    </div>
  </div>
</body>
</html>